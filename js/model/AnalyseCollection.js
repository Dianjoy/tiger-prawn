'use strict';
(function (ns) {
  ns.AnalyseCollection = ns.ListCollection.extend({
      initialize: function () {
        console.log("sdff");
      }
    }
  );
}(Nervenet.createNameSpace('tp.model')));